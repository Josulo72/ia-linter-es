# STATE

- **Último commit:** ver `git log -1`.
- **Terminado:** los seis bloques. B1 Base, B2 Motor, B3 Producto local, B4 Evidencia y voz humana, B5 Integraciones y B6 Cierre. Todos los gates en verde en Windows y en CI sobre Ubuntu, Windows y macOS con Node 20 y 22; evidencia en `project/EVIDENCE/B5-B6-cierre.md`, que es una foto del 2026-09-17 y por tanto dice 28 reglas stable: hoy son 25. La ejecución verde más reciente es la 35304438678, sobre el commit 40389c3.
- **Giro del producto (2026-09-17):** ya no se busca un detector. El objetivo es que la IA hable y escriba como una persona normal, integrado en el asistente, y que además arregle textos que ya huelen a IA. La referencia es el español cotidiano por situación, no el formal. Ver `docs/decisions.md`.
- **Lo que hay:**
  - **Guía de estilo** en `integrations/claude-code/output-styles/humano.md`. Es la pieza principal; el linter es el revisor.
  - **Corpus v1.1** (cotidiano): 36 mensajes de foro (Mediavida, elhacker.net, Infojardín; 2009-2021, no se redistribuyen, solo local) y 96 textos IA (48 base y 48 con la guía). Particiones 18+24, 18+24 y 0+48.
  - **Corpus v1.2** (correo, README y redes), en `corpus-v1.2/`: 90 textos humanos (30 por registro) y 180 IA (90 base y 90 con la guía), particiones 45+45, 45+45 y 0+90. Humanos: correo de la lista debian-user-spanish (2020-07 a 2021-12), READMEs de GitHub con licencia permisiva (2011-12 a 2021-10) y redes de Mastodon y Reddit (2020-08 a 2021-12). Todo anterior al corte 2022-01-01, solo en local, con manifiesto en `corpus-v1.2/manifests/`. Descarga reejecutable: `benchmark/scripts/fetch-registros.mjs`. Holdout congelado en `benchmark/configs/holdout-v1.2.lock`. Con `--from-manifest` se rehacen 24 de los 30 textos de redes: seis publicaciones de Mastodon ya no las sirve su instancia (pixelfed 401, mastodon.la 404, quey.org devuelve HTML, barcelona.social usa otra numeración). Correo, README y los 18 de Reddit se rehacen enteros.
  - **Corpus v1.0** archivado en `corpus/archive/v1.0` con sus manifiestos y su lock; `corpus-check` comprueba los tres.
  - **Reglas nuevas** (8, todas `candidate`): `estructura/ritmo-plano`, `estructura/ritmo-metronomo`, `retorica/no-es-x-es-y`, `formato/sentencia-dos-puntos`, `formato/raya`, `formato/comillas-angulares`, `formato/negrita-abre-parrafo`, `lexico/honestidad-anunciada`.
  - **Perfiles nuevos**: `chat`, `correo`, `readme` y `redes`, junto a los cuatro que ya había. `benchmark run` acepta `--profile`.
  - **Resultado**: holdout v1.1 con medianas 0 (humano) y 14,5 (IA), separación 14,5 ≥ 10, exactitud equilibrada 0,75. Informe en `benchmark/reports/v1.1.md`; el negativo de v1.0 sigue publicado en `v1.0.md`.
  - **Caso real**: el README de Josulo72/cazafacturas, reescrito con esta voz y subido a ese repositorio (commit 28074a6).
  - **Integraciones**: `.pre-commit-hooks.yaml` en la raíz, Action de node20 en `integrations/github-action` (anotaciones, salidas, resumen y SARIF) y el plugin de Claude Code en `integrations/claude-code` (estilo de salida, `/revisar`, skill y hook `Stop` apagado por defecto). Las tres dan los mismos hallazgos byte a byte.
  - **Cierre**: README, CHANGELOG, CONTRIBUTING, SECURITY, LICENSE (Business Source License 1.1 desde el commit 838ddc2, con vuelta a MIT el 2030-09-17; el texto de la MIT queda en `LICENSE-MIT`), `ia-linter.yml` propio y `scripts/pack-check.mjs`, que empaqueta, instala en un temporal y compara el resultado con el del repositorio.
- **Lo que hay que saber antes de seguir:**
  - **El corpus humano de v1.2 está contaminado y sus cifras no valen (2026-09-18).** El filtro de español de España aceptaba con una sola marca peninsular. En `readme` esa marca era circular: las consultas de GitHub buscan `ordenador`, `fichero`, `instalación`, `configuración`, `aplicación`, `pantalla botón usuario`, y esas mismas palabras eran las que aprobaban el texto, así que el filtro no podía rechazar nada de lo que la consulta devolvía. El filtro estricto (`habladeEspana`) solo corría sobre Reddit: los 30 README, los 30 correos y los 12 textos del fediverso no pasaron por él. No había filtro de prosa, así que podía entrar letra de canción o verso. `vale` contaba como marca peninsular, siendo el verbo valer. Y entre las fuentes de redes hay una instancia latinoamericana (`mastodon.la`) y tres textos de Pixelfed, que es red de fotos. Corregido en `registros-comun.mjs` y `fetch-registros.mjs`; `benchmark/scripts/auditar-corpus-humano.mjs` lista los textos que se caen, pero hay que ejecutarlo donde estén los textos. **Todo lo que sigue sobre v1.2 se midió sobre ese corpus.**
  - **Toda** la separación sigue viniendo de `estructura/ritmo-plano`, que es `candidate`, y ahora se sabe que solo funciona en un registro. En holdout v1.2 la separación es +18 en redes, **−9 en correo y −2 en README**: en esos dos el índice marca más a los textos humanos que a los generados. Contando solo las `stable`: 0 en correo, 0 en redes y −6 en README. La regla no se puede promover porque en correo su FP/1000 es 2,107, por encima del 1,5 de la política. Informe en `benchmark/reports/v1.2.md` y auditoría en `auditoria-banco-v1.2.md`.
  - **Reglas `stable`: 25** (eran 28). Bajan a `candidate` `densidad/conectores`, `lexico/desde-hasta-pasando` y `lexico/metaforas-comodin`, que no disparan en ninguna versión del corpus. Las otras tres que la auditoría de v1.1 señalaba sí aparecen en v1.2, aunque `densidad/verbos-comodin` solo lo hace en un README humano. Nueve `stable` siguen sin disparar en v1.2 y su FP sigue siendo 0 por vacío. Ninguna regla se ha promovido: sin adjudicación de hallazgos la precisión adjudicada es `null` y la política pide 0,70.
  - **Una regla `stable` incumple el umbral de falsos positivos y no se había registrado.** `repeticion/inicio-parrafo` da 2,019 FP/1000 en el holdout de README, por encima del 1,5 de la política, marcando 4 textos humanos y ninguno generado. Es la única `stable` que lo supera en los seis informes de v1.2. El gate no lo ve porque mide sobre el holdout v1.1, donde no dispara. Anotado el 2026-09-18 en `benchmark/reports/v1.2.md` y en la auditoría del banco; la regla no se ha tocado, porque bajarla a `candidate` deja las estables en 24, justo el mínimo, y apagarla en el perfil `readme` es un cambio de producto que decide el propietario.
  - **El script de auditoría medía mal.** `api.lintText` no tiene parámetro `profile`: las dos auditorías lo pasaban suelto, se ignoraba y el análisis acababa cargando el `ia-linter.yml` del repositorio. Corregido a `config: { profile }` en `auditoria-banco.mjs` y `auditoria-banco-v1.2.mjs`. La cifra de v1.1 no cambia (14,5); la de redes en v1.2 sí (de 12 a 0 la mediana humana).
  - El informe `benchmark/reports/holdout-v1.1.json`, que es el que lee `pnpm gates`, guarda el estado de las reglas de cuando se generó: allí las tres degradadas siguen apareciendo como `stable`. Corregido el 2026-09-18: el gate lee el estado del rulepack compilado y las métricas del informe congelado. Comprobado antes de cambiarlo que no altera ningún veredicto (ninguna regla incumple el FP con ninguno de los dos criterios); el texto pasa a decir «4 de 25».
  - **La documentación pública se había quedado en v1.1.** `README.md` y `docs/benchmark.md` seguían diciendo que los perfiles `correo`, `readme` y `redes` no tenían ni un caso medido y que había 28 reglas estables, y ninguno de los dos mencionaba v1.2. El `CHANGELOG.md` no recogía nada posterior a 1.0.0, incluido el cambio de licencia de MIT a BUSL-1.1. Los tres actualizados el 2026-09-18 con las cifras reales, incluida la inversión del índice en correo y en README.
  - **El ritmo no se arregla con la guía.** Se probaron cuatro versiones de la guía y nueve redacciones de la instrucción, ocho textos cada una: ninguna acerca el texto al humano más que no decir nada. La instrucción con números lo sobrecorrige y saca el defecto contrario (de ahí `estructura/ritmo-metronomo`). Decisión tomada: lo corrige el revisor. Ver `docs/decisions.md` y `benchmark/reports/v1.1.md`.
  - Las tandas de experimento (`corpus/challenge-v2`, `-v3`, `-v4` y `corpus/ritmo`, 144 textos) están registradas en `corpus/manifests/experimentos.yml` y las comprueba `corpus-check`. No entran en el benchmark.
  - El holdout v1.1 ya se ha ejecutado: no vale como evaluación independiente de una v1.2 de reglas. El de v1.2 también se ha ejecutado ya, una vez, con las reglas cerradas; para una v1.3 de reglas hace falta partición nueva.
  - Lo que sigue sin medir bien: la clase humana de redes mezcla 12 hilos de Mastodon con 18 publicaciones largas de Reddit, mientras que la clase IA son 30 hilos cortos, así que parte de la separación de redes puede ser de formato y no de voz. Los 18 de Reddit salen casi todos de r/es y r/askspain. Y no hay adjudicación de hallazgos en ninguna partición.
- **Bloqueos:** ninguno técnico. El push está hecho y el CI pasa en los tres sistemas. Pendiente del propietario, que es acción fuera del repositorio: `npm publish` (de él depende que funcione el hook de pre-commit `ia-linter-es`, que pide la versión publicada en `additional_dependencies`; hay que subirla en cada release), y cargar el plugin con `claude --plugin-dir ./integrations/claude-code` después de `pnpm build`, o copiar la guía a `~/.claude/output-styles/`.
- **Sin hacer, por decisión del propietario:** qué se hace con `repeticion/inicio-parrafo` (ver arriba) y si `api.lintText` debe rechazar claves desconocidas. Lo segundo es la causa raíz del fallo del instrumento: pasar `profile` fuera de `config` se sigue ignorando en silencio, y `packages/linter/test/api.test.ts` comprueba que el uso correcto funciona pero no que el incorrecto avise. Mientras no se cierre, el mismo error puede repetirse en cualquier script nuevo.
- **Próxima acción exacta (sustituye a la anterior):** ejecutar `node benchmark/scripts/auditar-corpus-humano.mjs --ver` donde estén los textos bajados, ver cuántos se caen por registro, y rehacer los tres con `node benchmark/scripts/fetch-registros.mjs --source <registro> --append` hasta volver a 30 por registro con el filtro corregido. Después hay que medir otra vez: el holdout de v1.2 ya se ha leído, así que la nueva medición necesita partición nueva. Hasta entonces no se toca ninguna regla con datos de v1.2.
- **Acción anterior, en espera hasta que el corpus esté limpio:** decidir qué se hace con `estructura/ritmo-plano` en `correo` y `readme`, donde está demostrado que no separa (en correo dispara en 8 de 15 humanos y 9 de 15 IA; en README, en 5 humanos y 3 IA). Apagarla en esos dos perfiles es un cambio de producto y lo decide el propietario; no se ha tocado. Después, adjudicar hallazgos en development v1.2 para poder promover o retirar reglas con la política en la mano.
- **Gates:** typecheck ✅, tests ✅ (77), build ✅, imports/reglas/rendimiento ✅ (25 stable), corpus ✅ (v1.2, v1.1 y v1.0), benchmark ✅ sobre holdout v1.1 (separación 14,5 ≥ 10; FP/1000 de reglas stable ✅), empaquetado ✅, `lint:self` ✅. El holdout v1.2 no es un gate: sus cifras se publican en el informe.
