# STATE

- **Último commit:** ver `git log -1`.
- **Terminado:** los seis bloques. B1 Base, B2 Motor, B3 Producto local, B4 Evidencia y voz humana, B5 Integraciones y B6 Cierre. Todos los gates en verde en Windows y en CI sobre Ubuntu, Windows y macOS con Node 20 y 22; evidencia en `project/EVIDENCE/B5-B6-cierre.md`.
- **Giro del producto (2026-09-17):** ya no se busca un detector. El objetivo es que la IA hable y escriba como una persona normal, integrado en el asistente, y que además arregle textos que ya huelen a IA. La referencia es el español cotidiano por situación, no el formal. Ver `docs/decisions.md`.
- **Lo que hay:**
  - **Guía de estilo** en `integrations/claude-code/guia/humano.md`. Es la pieza principal; el linter es el revisor.
  - **Corpus v1.1** (cotidiano): 36 mensajes de foro (Mediavida, elhacker.net, Infojardín; 2009-2021, no se redistribuyen, solo local) y 96 textos IA (48 base y 48 con la guía). Particiones 18+24, 18+24 y 0+48.
  - **Corpus v1.2** (correo, README y redes), en `corpus-v1.2/`: 90 textos humanos (30 por registro) y 180 IA (90 base y 90 con la guía), particiones 45+45, 45+45 y 0+90. Humanos: correo de la lista debian-user-spanish (2020-07 a 2021-12), READMEs de GitHub con licencia permisiva (2011-12 a 2021-10) y redes de Mastodon y Reddit (2020-08 a 2021-12). Todo anterior al corte 2022-01-01, solo en local, con manifiesto en `corpus-v1.2/manifests/`. Descarga reejecutable: `benchmark/scripts/fetch-registros.mjs`. Holdout congelado en `benchmark/configs/holdout-v1.2.lock`. Con `--from-manifest` se rehacen 24 de los 30 textos de redes: seis publicaciones de Mastodon ya no las sirve su instancia (pixelfed 401, mastodon.la 404, quey.org devuelve HTML, barcelona.social usa otra numeración). Correo, README y los 18 de Reddit se rehacen enteros.
  - **Corpus v1.0** archivado en `corpus/archive/v1.0` con sus manifiestos y su lock; `corpus-check` comprueba los tres.
  - **Reglas nuevas** (8, todas `candidate`): `estructura/ritmo-plano`, `estructura/ritmo-metronomo`, `retorica/no-es-x-es-y`, `formato/sentencia-dos-puntos`, `formato/raya`, `formato/comillas-angulares`, `formato/negrita-abre-parrafo`, `lexico/honestidad-anunciada`.
  - **Perfiles nuevos**: `chat`, `correo`, `readme` y `redes`, junto a los cuatro que ya había. `benchmark run` acepta `--profile`.
  - **Resultado**: holdout v1.1 con medianas 0 (humano) y 14,5 (IA), separación 14,5 ≥ 10, exactitud equilibrada 0,75. Informe en `benchmark/reports/v1.1.md`; el negativo de v1.0 sigue publicado en `v1.0.md`.
  - **Caso real**: el README de Josulo72/cazafacturas, reescrito con esta voz y subido a ese repositorio (commit 28074a6).
  - **Integraciones**: `.pre-commit-hooks.yaml` en la raíz, Action de node20 en `integrations/github-action` (anotaciones, salidas, resumen y SARIF) y el plugin de Claude Code en `integrations/claude-code`: guía cargada por `SessionStart`, `/revisar` y dos hooks de revisión apagados por defecto (`Stop` para respuestas y `PostToolUse` para archivos, con perfil por `situaciones.yml`). Sin skill de humanización ni estilo de salida desde B11. Para Codex y otros, `integrations/agents-md/`. Las tres dan los mismos hallazgos byte a byte.
  - **Cierre**: README, CHANGELOG, CONTRIBUTING, SECURITY, LICENSE (Business Source License 1.1, pasa a AGPL-3.0 en 2030; la 1.0.0 conserva la vuelta a MIT), `textoneitor.yml` propio y `scripts/pack-check.mjs`, que empaqueta, instala en un temporal y compara el resultado con el del repositorio.
- **Lo que hay que saber antes de seguir:**
  - **Toda** la separación sigue viniendo de `estructura/ritmo-plano`, que es `candidate`, y ahora se sabe que solo funciona en un registro. En holdout v1.2 la separación es +18 en redes, **−9 en correo y −2 en README**: en esos dos el índice marca más a los textos humanos que a los generados. Contando solo las `stable`: 0 en correo, 0 en redes y −6 en README. La regla no se puede promover porque en correo su FP/1000 es 2,107, por encima del 1,5 de la política. Informe en `benchmark/reports/v1.2.md` y auditoría en `auditoria-banco-v1.2.md`.
  - **Reglas `stable`: 23** desde el cierre de B9 (2026-09-18): `pregunta-retorica-apertura` y `encabezado-title-case` bajaron a `candidate` por precisión adjudicada y `stable_min` pasó a 23 (ver `docs/decisions.md`). Antes eran 25, y antes 28: en la auditoría de v1.2 bajaron a `candidate` `densidad/conectores`, `lexico/desde-hasta-pasando` y `lexico/metaforas-comodin`, que no disparan en ninguna versión del corpus. Las otras tres que la auditoría de v1.1 señalaba sí aparecen en v1.2, aunque `densidad/verbos-comodin` solo lo hace en un README humano. Nueve `stable` siguen sin disparar en v1.2 y su FP sigue siendo 0 por vacío. Ninguna regla se ha promovido: la adjudicación de development v1.2 (B9) no da a ninguna candidate 0,70 con muestra suficiente.
  - **El script de auditoría medía mal.** `api.lintText` no tiene parámetro `profile`: las dos auditorías lo pasaban suelto, se ignoraba y el análisis acababa cargando el `textoneitor.yml` del repositorio. Corregido a `config: { profile }` en `auditoria-banco.mjs` y `auditoria-banco-v1.2.mjs`. La cifra de v1.1 no cambia (14,5); la de redes en v1.2 sí (de 12 a 0 la mediana humana).
  - El informe `benchmark/reports/holdout-v1.1.json`, que es el que lee `pnpm gates`, guarda el estado de las reglas de cuando se generó: allí las tres degradadas siguen apareciendo como `stable`. No dispararon nunca, así que no cambia ningún resultado, pero el texto del gate dice «4 de 28» en vez de «4 de 25».
  - **El ritmo no se arregla con la guía.** Se probaron cuatro versiones de la guía y nueve redacciones de la instrucción, ocho textos cada una: ninguna acerca el texto al humano más que no decir nada. La instrucción con números lo sobrecorrige y saca el defecto contrario (de ahí `estructura/ritmo-metronomo`). Decisión tomada: lo corrige el revisor. Ver `docs/decisions.md` y `benchmark/reports/v1.1.md`.
  - Las tandas de experimento (`corpus/challenge-v2`, `-v3`, `-v4` y `corpus/ritmo`, 144 textos) están registradas en `corpus/manifests/experimentos.yml` y las comprueba `corpus-check`. No entran en el benchmark.
  - El holdout v1.1 ya se ha ejecutado: no vale como evaluación independiente de una v1.2 de reglas. El de v1.2 también se ha ejecutado ya, una vez, con las reglas cerradas; para una v1.3 de reglas hace falta partición nueva.
  - Lo que sigue sin medir bien: la clase humana de redes mezcla 12 hilos de Mastodon con 18 publicaciones largas de Reddit, mientras que la clase IA son 30 hilos cortos, así que parte de la separación de redes puede ser de formato y no de voz. Los 18 de Reddit salen casi todos de r/es y r/askspain. La adjudicación de development v1.2 son 50 hallazgos con un solo adjudicador, así que las precisiones por regla son orientativas.
- **Revisión correctiva (2026-09-19):** el gate aplica ahora la precisión mínima 0,70 al agregar development v1.2 cuando una regla alcanza cinco adjudicaciones. Las muestras menores quedan pendientes y las reglas globales se declaran exentas. Vitest queda estable en Node 24 con threads, un worker y archivos secuenciales. Evidencia en `project/EVIDENCE/revision-correctiva-2026-09-19.md`.
- **Bloqueos:** ninguno técnico. El push está hecho y el CI pasa en los tres sistemas. Pendiente del propietario, que es acción fuera del repositorio: `npm publish` (de él depende que funcione el hook de pre-commit `textoneitor`, que pide la versión publicada en `additional_dependencies`; hay que subirla en cada release), y cargar el plugin con `claude --plugin-dir ./integrations/claude-code` después de `pnpm build`. La guía ya no es un estilo de salida: la carga el plugin al empezar la sesión.
- **Fase 2 planificada (2026-09-18):** bloques B7 a B13 en `project/PLAN.md` y `project/WORK.yml`, B7 a B13 cerrados: versión 1.1.0, evidencia en `project/EVIDENCE/B7-B13-fase2.md`. Corpus v1.3 completo (180 textos de GPT-5.6 Sol) y holdout v1.3 congelado sin ejecutar. Principio fijado el mismo día: el linter no lleva IA dentro y la única IA del producto es el asistente que reescribe (ver `docs/decisions.md`).
- **Próxima acción exacta:** ampliar en development la muestra vigente de `retorica/triada` de dos a cinco casos únicos antes de interpretar su precisión. Después sigue pendiente del propietario `npm publish`. El holdout v1.3 continúa congelado y sin ejecutar.
- **Gates:** typecheck ✅, tests ✅ (4 del gate + 93 del linter, Node 24.12.0), build ✅, imports/reglas/rendimiento ✅ (23 stable), corpus ✅ (v1.3, v1.2, v1.1 y v1.0), benchmark ✅ sobre holdout v1.1 y adjudicaciones ciegas de development v1.2/v1.3. `retorica/triada` revisión 2 elimina cuatro falsos positivos; su muestra vigente queda en dos casos únicos. El holdout v1.3 sigue sin ejecutar.
